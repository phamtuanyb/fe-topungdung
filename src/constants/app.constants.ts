export const HEIGHT_HEADER_PUBLIC = 70 // 70px

/** Public URL prefix for service detail pages — `/{SERVICES_URL}/[slug]`. */
export const SERVICES_URL = 'dich-vu'
/** Category slug in DB for services (matches Category.slug column). */
export const SERVICES_SLUGS = 'services'
export const NEWS_SLUGS = 'tin-tuc'
export const AI_AGENT_SLUGS = 'ai-agent'